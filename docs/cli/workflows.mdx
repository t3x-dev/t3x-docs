---
slug: /cli/workflows
---

# CLI Workflows Preview

:::caution Preview surface
CLI workflow documentation is temporarily retired while the CLI contract is
still stabilizing.
:::

This page is kept for old links. Use [CLI Preview](../preview/cli) for the
current status and prefer `@t3x-dev/local` for the restricted alpha workflow.
